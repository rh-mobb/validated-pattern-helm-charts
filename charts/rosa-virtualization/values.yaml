namespace: openshift-cnv

subscription:
  name: kubevirt-hyperconverged
  channel: stable
  installPlanApproval: Manual
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: ""

operatorGroup:
  create: true
  name: kubevirt-hyperconverged-group

hyperConverged:
  create: true
  name: kubevirt-hyperconverged
  syncWave: 2
  featureGates:
    enableCommonBootImageImport: true
    deployTektonTaskResources: false
  liveMigrationConfig:
    bandwidthPerMigration: "64Mi"
    completionTimeoutPerGiB: 800
    parallelMigrationsPerCluster: 5
    parallelOutboundMigrationsPerNode: 2
    progressTimeout: 150
  resourceRequirements:
    vmiCPUAllocationRatio: 10
    # CDI clone/upload/import pods (propagates to CDIConfig podResourceRequirements).
    # Raise memory for large block-device clones (e.g. Fedora → EFS). Omit to use CNV defaults (~600M).
    storageWorkloads: {}

helper-status-checker:
  enabled: true
  approver: true
  checks:
    - operatorName: kubevirt-hyperconverged
      subscriptionName: kubevirt-hyperconverged
      namespace:
        name: openshift-cnv
      serviceAccount:
        name: "cnv-install-check"
