{{- if .Values.enabled }}
AutoNode sample NodePools installed.

NodePools rendered from values:
{{- range $idx, $pool := .Values.nodePools }}
  - {{ $pool.name }}
{{- if $pool.labels }}
    Scheduling labels:
{{- range $k, $v := $pool.labels }}
      * {{ $k }}={{ $v | quote }}
{{- end }}
    Example selector:
      nodeSelector:
{{- range $k, $v := $pool.labels }}
        {{ $k }}: {{ $v | quote }}
{{- end }}
{{- else }}
    (No labels set on this NodePool.)
{{- end }}
{{- end }}

Example Deployment targeting AutoNode-labeled nodes (autonode=true):
------------------------------------------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: autonode-smoke
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: autonode-smoke
  template:
    metadata:
      labels:
        app: autonode-smoke
    spec:
      nodeSelector:
        autonode: "true"
      containers:
        - name: stress
          image: progrium/stress@sha256:e34d56d60f5caae79333cee395aae93b74791d50e3841986420d23c2ee4697bf
          imagePullPolicy: IfNotPresent
          command: ["stress"]
          args: ["--cpu", "2", "--timeout", "600"]
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 128Mi
------------------------------------------------------------
{{- else }}
Chart is disabled (`enabled=false`), so no NodePools were rendered.
{{- end }}
