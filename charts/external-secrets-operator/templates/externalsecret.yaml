{{- /* Kuadrant/connectivity-link credentials sync — optional when only ClusterSecretStore is needed (e.g. BGP #51). */}}
{{- /* Use ne ... false so Helm `default` does not treat boolean false as empty. */}}
{{- if and .Values.serviceAccount.roleArn (ne .Values.target.enabled false) }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kuadrant-aws-credentials
  namespace: {{ .Values.target.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "6"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  refreshInterval: {{ .Values.target.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.target.secretName }}
    template:
      type: kuadrant.io/aws
      data:
        AWS_ACCESS_KEY_ID: "{{ "{{ .aws_access_key_id }}" }}"
        AWS_SECRET_ACCESS_KEY: "{{ "{{ .aws_secret_access_key }}" }}"
        AWS_REGION: "{{ "{{ .aws_region }}" }}"
  data:
    - secretKey: aws_access_key_id
      remoteRef:
        key: {{ .Values.secretStore.secretName }}
        property: AWS_ACCESS_KEY_ID
    - secretKey: aws_secret_access_key
      remoteRef:
        key: {{ .Values.secretStore.secretName }}
        property: AWS_SECRET_ACCESS_KEY
    - secretKey: aws_region
      remoteRef:
        key: {{ .Values.secretStore.secretName }}
        property: AWS_REGION
{{- end }}
